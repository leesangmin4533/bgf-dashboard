이제 충분한 정보를 수집했습니다. 종합 분석을 작성합니다.

---

## 운영 이상 분석 보고서 (2026-04-07)

**핵심 요약**: K2/K3 NO_DATA는 04-07 `collection.py` 구문 오류로 수집이 0일 — 당장 수정하면 다음 주기에 복구됨. K4=31은 `expiry_time_mismatch` 체크가 재설계(04-05) 이전부터 누적된 허위 양성일 가능성이 높음 — 체크 기준 재정의 필요. K1 단독 ACHIEVED는 정상.

---

### 1. KPI별 근본 원인

#### K1: 0.935 — ACHIEVED (정상)
`eval_outcomes`에서 mae_7d/mae_14d = 0.935 < 1.05 목표 달성. **별도 조치 불필요.**

단, `prediction.md` PLANNED P1에 카테고리 015·039·040·048의 7d MAE 61% 악화가 기록됨. K1은 전체 평균이므로 통과했지만 특정 카테고리 불안정은 진행 중.

---

#### K2: None — NO_DATA
**원인**: `OpsMetrics._waste_rate()`가 `daily_sales` 최근 30일 중 7일 이상 데이터를 요구하는데, **04-07 `collection.py` 구문 오류로 daily_job이 전체 실패** (`OPEN` 이슈, expiry-tracking.md) → 판매 데이터 수집 중단 → `daily_sales` 공백 발생.

```
_waste_rate() 분기:
  data_days = COUNT(DISTINCT sales_date) WHERE >= date('now', '-30 days')
  if data_days < 7 → {"insufficient_data": True}  ← 여기서 차단
```

**구조적 취약점**: K2는 `waste_slip_items`(폐기 전표)과 `daily_sales` 양쪽 모두 필요. 수집 오류 하나가 KPI 전체를 NO_DATA로 만드는 구조.

---

#### K3: None — NO_DATA
**원인**: 동일. `_order_failure()`가 `order_fail_reasons.eval_date` 기반 7일 확인 → 수집 실패로 데이터 없음. `order_history`도 07:00 daily_job 실패로 신규 입력 없음.

추가 확인: `order_fail_reasons`에 `eval_date` 컬럼이 실제로 있는지 확인 필요. `order_fail_reasons` 테이블은 `fail_reason_collector.py`가 채우는데, 이것도 collection 실패 영향권.

---

#### K4: 31 — NOT_MET (가장 심각)
**원인**: `DataIntegrityService`의 `expiry_time_mismatch` 체크가 31일 연속 anomaly > 0 반환.

```python
# milestone_tracker.py:231-234
for check in integrity.get("checks", []):
    days = check.get("consecutive_days", 0)
    if days > max_consecutive:
        max_consecutive = days  # → 31
```

**핵심 가설**: 폐기 추적이 04-05에 **`confirmed_orders` + `delivery_match` 기반**으로 재설계(9aca17d)되었는데, `expiry_time_mismatch` 체크는 **구(舊) OT 기반** 로직(`order_tracking.expiry_time` vs `inventory_batches.expiry_datetime` 비교)을 여전히 사용 중. 재설계 이후 OT의 `expiry_time`은 의도적으로 갱신하지 않는 구조이므로, **체크 자체가 허위 양성**을 대량 생성하는 상황일 가능성.

---

### 2. 이슈체인 PLANNED 연관 항목

| 이슈체인 | 항목 | 우선순위 | K4 연관 |
|----------|------|----------|---------|
| `scheduling.md` | 자전 시스템 미해결 항목 (expiry_time_mismatch) | **P1** | **직접 원인** |
| `scheduling.md` | CLEAR_GHOST_STOCK 자동실행 승격 검토 | P2 | 간접 |
| `prediction.md` | 예측 정확도 하락 조사 (4개 카테고리) | **P1** | K1 잠재 위험 |
| `prediction.md` | ML is_payday DB 반영 효과 검증 | P2 | K1 관련 |

---

### 3. 즉시 적용 가능한 대응 방안

#### 우선순위 1 — collection.py 구문 오류 수정 (K2/K3 복구)
`docs/05-issues/expiry-tracking.md`에 `[OPEN]`으로 등록된 04-07 collection.py 오류 수정이 **K2·K3 복구의 전제조건**. 수집이 정상화되면 약 7일 후 자동으로 NO_DATA 해소.

#### 우선순위 2 — `expiry_time_mismatch` 체크 재정의 (K4 해소)
`DataIntegrityService`에서 이 체크가 구체적으로 무엇을 비교하는지 확인 후:
- **Case A (허위 양성)**: 04-05 재설계 이후 OT `expiry_time`을 갱신 안 하는 것이 설계라면, 체크 기준을 `inventory_batches.expiry_datetime` 단독으로 변경하거나 체크 비활성화
- **Case B (실제 문제)**: `inventory_batches.expiry_datetime`이 실제로 잘못 계산되는 케이스를 찾아 수정

확인 위치: `src/analysis/` 또는 `src/scheduler/` 내 `DataIntegrityService` 구현체, `expiry_time_mismatch` 체크 로직.

#### 우선순위 3 — 예측 정확도 4개 카테고리 조사 (K1 방어)
현재 K1은 통과했지만 015·039·040·048의 MAE 61% 악화가 지속되면 다음 주에 K1도 ACHIEVED → NOT_MET 전락 가능. `prediction.md` PLANNED P1 항목 착수 권장.

---

### 4. 코드 변경 방향 (수정은 안 함)

**K4 관련 핵심 조사 파일**:
```
src/analysis/data_integrity_service.py (또는 유사 파일)
  → expiry_time_mismatch 체크 정의 위치
  → 비교 대상 컬럼 확인

src/infrastructure/database/repos/ 내 integrity_checks 관련 repo
  → consecutive_days 계산 방식 확인
```

**K2/K3 구조 강화 방향**: 수집 실패 시 `insufficient_data` 대신 "마지막 유효 데이터 사용" 폴백을 추가하면 단발 수집 실패로 KPI 전체가 NO_DATA가 되는 문제 방지 가능. (`ops_metrics.py` `_MIN_DATA_DAYS` 로직 완화 또는 폴백 추가)

---

### 5. 신규 이슈 제안

기존 이슈체인이 모든 항목을 커버하고 있음 — **신규 이슈 불필요**.

- K2/K3 NO_DATA → `collection.py` 구문 오류(`expiry-tracking.md` OPEN) 해결로 자연 해소
- K4 31일 → `scheduling.md` PLANNED P1 `expiry_time_mismatch` 항목이 정확히 대응
- 예측 4개 카테고리 → `prediction.md` PLANNED P1 기존 등록

---

─────────────────────────────────────────────────
📊 bkit Feature Usage
─────────────────────────────────────────────────
✅ Used: Explore agent (DB 구조·이슈체인 병렬 탐색), Read (ops_metrics.py, milestone_tracker.py, 이슈체인 파일 직접 확인)
⏭️ Not Used: gap-detector (설계-구현 비교 불필요), pdca-iterator (수정 작업 없음), code-analyzer (분석 요청만)
💡 Recommended: `/pdca do expiry_time_mismatch` — K4 해소를 위한 체크 재정의 구현 착수
─────────────────────────────────────────────────
