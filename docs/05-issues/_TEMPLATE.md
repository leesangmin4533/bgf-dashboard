# {영역명} 이슈 체인

> 최종 갱신: {YYYY-MM-DD}
> 현재 상태: {한줄 요약}

---

## [{OPEN|RESOLVED|WATCHING}] {이슈 제목} ({시작일} ~ {종료일 또는 진행중})

**문제**: {무엇이 잘못됐는지}
**영향**: {수치 포함}
**설계 의도**: {수정 과정에서 변하면 안 되는 핵심 원칙}
**연관**: → {파일}#{이슈명} (없으면 삭제)

### 시도 1: {한줄 요약} ({커밋해시}, {MM-DD})
- **왜**: {이 접근을 선택한 판단 근거}
- **결과**: ✗ {왜 안 됐는지} / ✓
- **실패 패턴**: #{태그}

> 변경 내용은 `git show {커밋해시}`로 확인. 여기에 diff를 중복 기술하지 않는다.

### 의도 점검 (시도 3회 이상 실패 시 필수)
- [ ] 원래 설계 의도를 아직 따르고 있는가?
- [ ] 패치가 아닌 구조 재설계가 필요한가?
- [ ] 현재 접근의 근본 가정이 여전히 유효한가?
> 점검 결과: {유지 / 의도 전환 — 전환 시 설계 의도를 위에 갱신}

### 교훈
- {왜 특정 접근이 안 되는지 — 다음 수정자를 위한 핵심}

### 해결: {한줄 요약} ({커밋해시}, {MM-DD})
- 검증:
  - [ ] {체크포인트} (예정: {MM-DD}, 스케줄: {태스크ID 또는 수동})

---

<!-- 역할 구분
  커밋 메시지 = "뭘 바꿨는지" (변경 사항, 영향 범위, 테스트 결과)
  이슈 체인   = "왜 이렇게 됐는지" (판단 근거, 시도 간 인과관계, 교훈, 검증)
  중복 금지: 이슈 체인에서 커밋 해시로 참조, diff 복사 안 함
-->

<!-- 실패 패턴 태그 (3회 이상 반복 시에만 새 태그 추가)
#stale-data              오래된 데이터로 인한 오판
#data-format             데이터 형식 불일치
#no-consumption-tracking 소진 추적 없는 데이터 소스
#timing-gap              수집 시점 간 시차
#cross-contamination     매장 간 데이터 오염
#one-shot-fix            일회성 수정, 자동 반복 안 됨
#patch-on-patch          근본 해결 없이 패치 누적
-->
