[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -n auto
markers =
    unit: 순수 로직 유닛 테스트 (DB 불필요)
    db: DB 의존 테스트 (in-memory SQLite)
    slow: 느린 테스트 (평소 스킵: -m "not slow")
